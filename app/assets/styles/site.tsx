import { StyleSheet } from "react-native";

const SiteStyles = StyleSheet.create({
	cardTitle: {
		marginBottom: "4px",
		color: "#111",
	},
	compactCard: {
		padding: "8px",
		margin: "2px",
		alignContent: "center",
	},
});

export default SiteStyles;
